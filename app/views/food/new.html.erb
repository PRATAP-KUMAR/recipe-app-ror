Create Food

<%= form_with model: @food, url: food_new_path, local: true do |f| %>
    <%= f.label :name %>
    <%= f.text_field :title %>

    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.submit 'Submit' %>
<% end %>