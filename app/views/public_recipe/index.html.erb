<% @recipes.each_with_index do |recipe, idx| %>
    <div class=index-container>
      <div class=recipe-card>
        <div class=left-box>
          <h2><%= recipe.name %></h2>
        </div>
        <div class=right-box>
          <%= recipe.description %>
        </div>
      </div>
    </div>
<%end%>